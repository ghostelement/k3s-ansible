apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.iglSiteApp.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  
  type: ClusterIP
  ports:
    - port: {{ .Values.image.iglSiteApp.port }}
      targetPort: {{ .Values.image.iglSiteApp.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.iglSiteApp.name }}
