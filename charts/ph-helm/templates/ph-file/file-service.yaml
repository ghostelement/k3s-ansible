apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.phFile.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.phFile.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.phFile.port }}
      targetPort: {{ .Values.image.phFile.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.phFile.name }}
