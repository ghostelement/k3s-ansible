apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.phSystem.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.phSystem.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.phSystem.port }}
      targetPort: {{ .Values.image.phSystem.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.phSystem.name }}
