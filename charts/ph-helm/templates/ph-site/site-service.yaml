apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.phSite.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  {{- if eq .Values.image.phSite.service.type "NodePort" }}
  type: NodePort
  ports:
    - port: {{ .Values.image.phSite.port }}
      targetPort: {{ .Values.image.phSite.port }}
      nodePort: {{ .Values.image.phSite.service.externalPort }}
      protocol: TCP
      name: http
  {{- else }}
  type: ClusterIP
  ports:
    - port: {{ .Values.image.phSite.port }}
      targetPort: {{ .Values.image.phSite.port }}
      protocol: TCP
      name: http
  {{- end }}
  selector:
    app: {{ .Values.image.phSite.name }}
