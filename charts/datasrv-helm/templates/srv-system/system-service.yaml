apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.srvSystem.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.srvSystem.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.srvSystem.port }}
      targetPort: {{ .Values.image.srvSystem.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.srvSystem.name }}
