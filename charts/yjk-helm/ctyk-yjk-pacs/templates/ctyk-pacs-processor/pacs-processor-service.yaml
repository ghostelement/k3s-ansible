apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.pacsProcessor.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.pacsProcessor.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.pacsProcessor.port }}
      targetPort: {{ .Values.image.pacsProcessor.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.pacsProcessor.name }}
