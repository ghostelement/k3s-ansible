{{- if .Values.modules.nursemobile.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.nursemobile.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.nursemobile.port }}
      targetPort: {{ .Values.image.nursemobile.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.nursemobile.name }}
{{- end }}