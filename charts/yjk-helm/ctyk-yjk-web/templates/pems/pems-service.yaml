{{- if .Values.modules.pems.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.pems.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.pems.port }}
      targetPort: {{ .Values.image.pems.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.pems.name }}
{{- end }}