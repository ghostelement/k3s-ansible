{{- if .Values.modules.mzinfusion.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.mzinfusion.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.mzinfusion.port }}
      targetPort: {{ .Values.image.mzinfusion.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.mzinfusion.name }}
{{- end }}