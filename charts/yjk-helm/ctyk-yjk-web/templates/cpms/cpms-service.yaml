{{- if .Values.modules.cpms.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.cpms.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.cpms.port }}
      targetPort: {{ .Values.image.cpms.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.cpms.name }}
{{- end }}