{{- if and (eq .Values.service.type "Ingress") (.Values.modules.cpms.enabled) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.image.cpms.name }}
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: {{ .Values.service.ingressClassName }}
  rules:
  - host: {{ .Values.service.domain }}
    http:
      paths:
      - path: /cpms
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.image.cpms.name }}  ## 后端service服务名称
            port:
              number: {{ .Values.image.cpms.port }}
{{- end }}