{{- if .Values.modules.cmis.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.cmis.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.cmis.port }}
      targetPort: {{ .Values.image.cmis.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.cmis.name }}
{{- end }}