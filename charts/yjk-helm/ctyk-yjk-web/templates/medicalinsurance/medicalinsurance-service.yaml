{{- if .Values.modules.medicalinsurance.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.medicalinsurance.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.medicalinsurance.port }}
      targetPort: {{ .Values.image.medicalinsurance.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.medicalinsurance.name }}
{{- end }}