{{- if and (eq .Values.service.type "Ingress") (.Values.modules.tigcoac.enabled) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.image.tigcoac.name }}
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: {{ .Values.service.ingressClassName }}
  rules:
  - host: {{ .Values.service.domain }}
    http:
      paths:
      - path: /tigcoac
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.image.tigcoac.name }}  ## 后端service服务名称
            port:
              number: {{ .Values.image.tigcoac.port }}
      - path: /tigcpacs
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.image.tigcoac.name }}
            port:
              number: {{ .Values.image.tigcoac.port }}
      - path: /tigcdrug
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.image.tigcoac.name }}
            port:
              number: {{ .Values.image.tigcoac.port }}
      - path: /tigclis
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.image.tigcoac.name }}
            port:
              number: {{ .Values.image.tigcoac.port }}
{{- end }}