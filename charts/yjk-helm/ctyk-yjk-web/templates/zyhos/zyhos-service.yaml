{{- if .Values.modules.zyhos.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.zyhos.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.zyhos.port }}
      targetPort: {{ .Values.image.zyhos.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.zyhos.name }}
{{- end }}