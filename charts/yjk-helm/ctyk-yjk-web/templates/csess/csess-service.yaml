{{- if .Values.modules.csess.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.csess.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.csess.port }}
      targetPort: {{ .Values.image.csess.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.csess.name }}
{{- end }}