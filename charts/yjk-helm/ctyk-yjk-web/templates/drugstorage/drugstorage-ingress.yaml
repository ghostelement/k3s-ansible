{{- if and (eq .Values.service.type "Ingress") (.Values.modules.drugstorage.enabled) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.image.drugstorage.name }}
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: {{ .Values.service.ingressClassName }}
  rules:
  - host: {{ .Values.service.domain }}
    http:
      paths:
      - path: /drugstorage
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.image.drugstorage.name }}  ## 后端service服务名称
            port:
              number: {{ .Values.image.drugstorage.port }}
{{- end }}