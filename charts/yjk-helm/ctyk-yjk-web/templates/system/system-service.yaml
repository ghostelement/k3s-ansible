apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.system.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.system.port }}
      targetPort: {{ .Values.image.system.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.system.name }}