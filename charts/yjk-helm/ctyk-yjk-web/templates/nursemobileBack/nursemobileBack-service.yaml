{{- if .Values.modules.nursemobileBack.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.nursemobileBack.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.nursemobileBack.port }}
      targetPort: {{ .Values.image.nursemobileBack.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.nursemobileBack.name }}
{{- end }}