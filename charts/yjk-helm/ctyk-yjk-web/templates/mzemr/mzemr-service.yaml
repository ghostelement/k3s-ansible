{{- if .Values.modules.mzemr.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.mzemr.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.mzemr.port }}
      targetPort: {{ .Values.image.mzemr.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.mzemr.name }}
{{- end }}