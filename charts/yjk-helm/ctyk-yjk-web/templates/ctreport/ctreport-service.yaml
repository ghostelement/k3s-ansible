apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.ctreport.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.ctreport.port }}
      targetPort: {{ .Values.image.ctreport.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.ctreport.name }}