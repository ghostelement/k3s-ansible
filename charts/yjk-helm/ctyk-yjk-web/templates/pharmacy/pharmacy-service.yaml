{{- if .Values.modules.pharmacy.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.pharmacy.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.pharmacy.port }}
      targetPort: {{ .Values.image.pharmacy.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.pharmacy.name }}
{{- end }}