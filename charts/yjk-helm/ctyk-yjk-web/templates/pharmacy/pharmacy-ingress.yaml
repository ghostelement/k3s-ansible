{{- if and (eq .Values.service.type "Ingress") (.Values.modules.pharmacy.enabled) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.image.pharmacy.name }}
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: {{ .Values.service.ingressClassName }}
  rules:
  - host: {{ .Values.service.domain }}
    http:
      paths:
      - path: /pharmacy
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.image.pharmacy.name }}  ## 后端service服务名称
            port:
              number: {{ .Values.image.pharmacy.port }}
{{- end }}