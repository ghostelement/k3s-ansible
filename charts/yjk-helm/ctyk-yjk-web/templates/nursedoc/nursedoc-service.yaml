{{- if .Values.modules.nursedoc.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.nursedoc.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.nursedoc.port }}
      targetPort: {{ .Values.image.nursedoc.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.nursedoc.name }}
{{- end }}