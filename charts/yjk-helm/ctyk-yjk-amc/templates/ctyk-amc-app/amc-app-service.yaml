apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.amcApp.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.amcApp.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.amcApp.port }}
      targetPort: {{ .Values.image.amcApp.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.amcApp.name }}
