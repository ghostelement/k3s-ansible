{{- if .Values.modules.dqs.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.dqs.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.dqs.port }}
      targetPort: {{ .Values.image.dqs.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.dqs.name }}
{{- end }}