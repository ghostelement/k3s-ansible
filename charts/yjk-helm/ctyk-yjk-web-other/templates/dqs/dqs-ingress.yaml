{{- if and (eq .Values.service.type "Ingress") (.Values.modules.dqs.enabled) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.image.dqs.name }}
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: {{ .Values.service.ingressClassName }}
  rules:
  - host: {{ .Values.service.domain }}
    http:
      paths:
      - path: /dqs
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.image.dqs.name }}  ## 后端service服务名称
            port:
              number: {{ .Values.image.dqs.port }}
{{- end }}