{{- if .Values.modules.tigcoac.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.tigcoac.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.tigcoac.port }}
      targetPort: {{ .Values.image.tigcoac.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.tigcoac.name }}
{{- end }}