{{- if .Values.modules.amc.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.amc.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.amc.port }}
      targetPort: {{ .Values.image.amc.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.amc.name }}
{{- end }}