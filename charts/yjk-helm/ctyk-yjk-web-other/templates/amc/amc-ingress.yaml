{{- if and (eq .Values.service.type "Ingress") (.Values.modules.amc.enabled) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.image.amc.name }}
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: {{ .Values.service.ingressClassName }}
  rules:
  - host: {{ .Values.service.domain }}
    http:
      paths:
      - path: /amc
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.image.amc.name }}  ## 后端service服务名称
            port:
              number: {{ .Values.image.amc.port }}
{{- end }}