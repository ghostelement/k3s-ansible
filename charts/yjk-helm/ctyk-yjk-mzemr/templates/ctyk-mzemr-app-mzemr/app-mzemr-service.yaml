apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.appMzemr.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.appMzemr.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.appMzemr.port }}
      targetPort: {{ .Values.image.appMzemr.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.appMzemr.name }}
