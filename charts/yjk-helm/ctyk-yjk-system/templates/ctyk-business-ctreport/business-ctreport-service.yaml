apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.businessCtreport.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.businessCtreport.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.businessCtreport.port }}
      targetPort: {{ .Values.image.businessCtreport.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.businessCtreport.name }}
