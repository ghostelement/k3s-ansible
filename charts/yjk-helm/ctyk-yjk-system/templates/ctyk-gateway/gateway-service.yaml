apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.ctykGateway.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.ctykGateway.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.ctykGateway.port }}
      targetPort: {{ .Values.image.ctykGateway.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.ctykGateway.name }}
