apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.ctykAuth.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.ctykAuth.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.ctykAuth.port }}
      targetPort: {{ .Values.image.ctykAuth.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.ctykAuth.name }}
