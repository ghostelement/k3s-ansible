apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.aggregateDrug.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.aggregateDrug.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.aggregateDrug.port }}
      targetPort: {{ .Values.image.aggregateDrug.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.aggregateDrug.name }}
