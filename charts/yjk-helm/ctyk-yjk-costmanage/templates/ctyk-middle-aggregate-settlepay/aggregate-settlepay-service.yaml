apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.aggregateSettlepay.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.aggregateSettlepay.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.aggregateSettlepay.port }}
      targetPort: {{ .Values.image.aggregateSettlepay.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.aggregateSettlepay.name }}
