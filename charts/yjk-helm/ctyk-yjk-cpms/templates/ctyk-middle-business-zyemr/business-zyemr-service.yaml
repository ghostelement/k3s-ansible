apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.businessZyemr.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.businessZyemr.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.businessZyemr.port }}
      targetPort: {{ .Values.image.businessZyemr.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.businessZyemr.name }}
