apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.busiNursemobile.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.busiNursemobile.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.busiNursemobile.port }}
      targetPort: {{ .Values.image.busiNursemobile.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.busiNursemobile.name }}
