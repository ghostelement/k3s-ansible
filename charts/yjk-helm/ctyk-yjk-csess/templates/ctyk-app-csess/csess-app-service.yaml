apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.csessApp.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.csessApp.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.csessApp.port }}
      targetPort: {{ .Values.image.csessApp.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.csessApp.name }}
