apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.appHmms.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.appHmms.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.appHmms.port }}
      targetPort: {{ .Values.image.appHmms.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.appHmms.name }}
