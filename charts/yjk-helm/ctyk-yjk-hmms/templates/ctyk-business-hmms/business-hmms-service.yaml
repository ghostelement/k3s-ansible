apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.busHmms.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.busHmms.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.busHmms.port }}
      targetPort: {{ .Values.image.busHmms.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.busHmms.name }}
