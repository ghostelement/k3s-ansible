apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.ygtWeb.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
spec:
  {{- if eq .Values.image.ygtWeb.service.type "NodePort" }}
  type: NodePort
  ports:
    - port: {{ .Values.image.ygtWeb.port }}
      targetPort: {{ .Values.image.ygtWeb.port }}
      nodePort: {{ .Values.image.ygtWeb.service.externalPort }}
      protocol: TCP
      name: http
  {{- else }}
  type: ClusterIP
  ports:
    - port: {{ .Values.image.ygtWeb.port }}
      targetPort: {{ .Values.image.ygtWeb.port }}
      protocol: TCP
      name: http
  {{- end }}
  selector:
    app: {{ .Values.image.ygtWeb.name }}
