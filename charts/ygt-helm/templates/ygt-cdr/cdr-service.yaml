{{- if .Values.cdr }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.ygtCdr.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.ygtCdr.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.ygtCdr.port }}
      targetPort: {{ .Values.image.ygtCdr.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.ygtCdr.name }}
{{- end }}