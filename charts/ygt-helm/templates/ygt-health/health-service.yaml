{{- if .Values.health }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.ygtHealth.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.ygtHealth.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.ygtHealth.port }}
      targetPort: {{ .Values.image.ygtHealth.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.ygtHealth.name }}
{{- end }}