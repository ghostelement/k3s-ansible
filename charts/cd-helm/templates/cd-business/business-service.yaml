apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.cdBusiness.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.cdBusiness.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.cdBusiness.port }}
      targetPort: {{ .Values.image.cdBusiness.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.cdBusiness.name }}
