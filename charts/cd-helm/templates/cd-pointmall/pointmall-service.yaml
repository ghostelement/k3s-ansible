apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.image.cdPointmall.name }}
  labels:
    {{- include "ctyk-yjk.labels" . | nindent 4 }}
  annotations:
    prometheus.io/port: {{ .Values.image.cdPointmall.port | quote }}  #prometheus监控端口
    prometheus.io/spring: "true"
    prometheus.io/path: "actuator/prometheus"
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.image.cdPointmall.port }}
      targetPort: {{ .Values.image.cdPointmall.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.image.cdPointmall.name }}
